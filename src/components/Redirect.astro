---
// A static permanent-move page: meta refresh plus canonical, for URLs that moved (e.g. /hubs/ → /h/).
interface Props {
  to: string;
}
const { to } = Astro.props;
const abs = new URL(to, Astro.site).toString();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Moved</title>
    <meta name="robots" content="noindex" />
    <link rel="canonical" href={abs} />
    <meta http-equiv="refresh" content={`0; url=${to}`} />
    <script is:inline define:vars={{ to }}>location.replace(to + location.hash);</script>
  </head>
  <body>
    <a href={to}>This page moved to {to}</a>
  </body>
</html>
